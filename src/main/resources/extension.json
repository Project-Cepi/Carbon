{
  "entrypoint": "${project.group}.${project.mainClass}",
  "name": "${project.name}",
  "version": "${project.version}",
  "depdencies": [
    "KStom"
  ],
  "meta": {
    "downloadURL": "https://github.com/Project-Cepi/Carbon/releases/download/latest/CarbonExtension-1.0.0-all.jar"
  }
}